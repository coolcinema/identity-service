version: 1.0.0
metadata:
  name: IdentityService
  slug: identity-service
  description: Service description
grpc:
  main:
    files:
      - src/proto/identity.proto
    port: 5000
ingress:
  main:
    port: 3000
    path: /
